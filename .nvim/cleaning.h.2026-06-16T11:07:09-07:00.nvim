#pragma once

// Splits an input into words using strtok_r
int split(char *input, char *words[], int max_words);

// Trims each string of whitespace
void trim(char *s);

int readInput(char *readBuff, char *dirBuff, bool *background);
